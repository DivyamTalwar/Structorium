[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "structorium"
version = "0.8.0"
description = "Architecture. Automatically Enforced"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    {name = "Peter O'Malley", email = "pom@banodoco.ai"},
]
keywords = ["code-quality", "technical-debt", "linter", "static-analysis", "refactoring"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",

    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Quality Assurance",
    "Topic :: Software Development :: Testing",
    "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/peteromallet/structorium"
Repository = "https://github.com/peteromallet/structorium"
Issues = "https://github.com/peteromallet/structorium/issues"

[project.optional-dependencies]
treesitter = ["tree-sitter-language-pack>=0.3"]
python-security = ["bandit>=1.7.8"]
scorecard = ["Pillow>=9.0.0"]
ai = [
  "neo4j>=5.20.0",
  "turbopuffer>=0.6.0",
]
full = [
  "tree-sitter-language-pack>=0.3",
  "bandit>=1.7.8",
  "Pillow>=9.0.0",
  "neo4j>=5.20.0",
  "turbopuffer>=0.6.0",
]

[project.scripts]
structorium = "cli:main"

[tool.setuptools.packages.find]
include = ["app*", "core*", "engine*", "intelligence*", "languages*"]
exclude = ["tests", "tests.*"]

[tool.setuptools]
include-package-data = false
py-modules = [
  "cli",
  "file_discovery",
  "hook_registry",
  "scoring",
  "search",
  "state",
  "utils",
  "versioning",
]

[tool.setuptools.package-data]
"languages._framework" = ["review_data/*.json"]
"languages.python" = ["review_data/*.json"]
"languages.typescript" = ["review_data/*.json"]
"languages.csharp" = ["review_data/*.json"]
"languages.dart" = ["review_data/*.json"]
"languages.gdscript" = ["review_data/*.json"]
"languages.go" = ["review_data/*.json"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ['tests', 'languages/python/tests', 'languages/typescript/tests', 'languages/csharp/tests', 'languages/dart/tests', 'languages/gdscript/tests', 'languages/go/tests']
norecursedirs = ["tests/fixtures"]

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
warn_redundant_casts = true
warn_unreachable = true
show_error_codes = true
strict_optional = true
ignore_missing_imports = true
follow_imports = "skip"
pretty = true
files = [
  "app/commands/review/entrypoint.py",
  "app/commands/review/prepare.py",
  "app/commands/review/batches.py",
  "app/commands/review/batch_core.py",
  "app/commands/review/import_cmd.py",
  "app/commands/review/runtime.py",
  "app/commands/scan/scan_reporting_dimensions.py",
  "app/commands/scan/scan_reporting_llm.py",
  "app/commands/scan/scan_reporting_presentation.py",
  "app/commands/scan/scan_reporting_subjective.py",
  "app/commands/scan/scan_workflow.py",
  "app/cli_support/parser.py",
  "app/cli_support/parser_groups.py",
  "engine/concerns.py",
  "languages/_framework/generic.py",
]
